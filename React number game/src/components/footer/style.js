import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  root: {
    background: "#3f51b5",
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    color: "white",
    padding: "0 1rem",
    textAlign: "center"
  }
});
