import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  root: {
    background: "yellow",
    padding: "2rem",
    borderRadius: "10px",
    width: "300px",
    margin: ".5rem"
  }
});
